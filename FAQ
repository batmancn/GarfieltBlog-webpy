### 运行index.py时候端口被占用？

如果在运行index.py时候出现端口被占用（raise socket.error），此时只要在命令后面指定端口即可。命令：python index.py 8888。其中8888是端口号。


### 如何后台运行？

nohup commands &


### 如何进入管理界面？

在setting.py中，有个login_url，通过这个url进入管理界面。


### Garfilt的manage/install界面显示的sqlite语句有问题？

是的，正确的应该是sqlite.patch中所写，就是把PRIMARY KEY ASC定义到主键后面。


### 管理界面的用户名密码是什么?

admin/admin
